完成本轮 11 项审查问题修复，面向 World of Warcraft Retail 12.1.0。

- 修正尖刺钉螺的临时附魔追踪与晶华感知瓶剂的 Aura ID，迁移旧默认条目。
- 处理受限 Aura 字段；无法确认的效果、时间或层数显示未知状态。
- 修复战斗中显示与点击动作不一致、编辑草稿丢失、Settings 布局越界和拖动恢复问题。
- 复用安全按钮，限制无效字段编辑，校验并恢复损坏的保存配置。
- 合并到期扫描，持续过期数据的自动重试间隔至少一秒。

本地验证：Lua 5.1 完整回归 **177 通过、0 失败**；Smoke、12 组固定版本
Blizzard 源码契约及语法检查通过。

真实客户端验收尚未完成：Aura 393714、slot 28 附魔与一键宏、实际物品点击、
secret/taint、战斗交互和最终视觉效果仍需进游戏确认。

安装：下载 `FishingBuffTracker-v0.3.0.zip`，解压后将 `FishingBuffTracker`
目录放入 `_retail_/Interface/AddOns/`，再执行 `/reload`。ZIP 和 tar.gz
包含相同的插件运行文件，可用 `SHA256SUMS` 校验。

保存配置会升级至版本 2，保留有效条目与用户偏好；升级前可备份原有
`FishingBuffTrackerDB`。
