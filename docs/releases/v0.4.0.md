完成 Retail 12.1.0 客户端实机验收与易用性更新。

- 修复受限 Aura 场景中 `AuraUtil.GetAuraSlots()` 引发的 taint，改为按
  SpellID 定向读取效果。
- 战斗中请求打开设置面板时延迟到脱战执行，避免保护功能报错。
- 修复拖动过程中进入战斗后保存错误鼠标位置的问题。
- 优化状态栏圆角、内边距、按钮居中与锁定状态自适应布局。
- 增加 Buff 条目配置导出和导入，使用可读的 `FBT-ENTRIES-1` 文本格式。

实机验证环境：Retail `12.1.0.69814`。物品、Toy、尖刺钉螺一键宏与
EnchantID 8675、诱饵显隐、战斗设置切换、点击触发模式、受限 Aura、
拖动恢复和持续运行均已通过。晶华感知瓶剂 ItemID 191354 / Aura 393714
因本轮没有对应物品，按计划暂缓确认。

本地验证：Lua 5.1 完整回归 **181 通过、0 失败**；Smoke、语法检查和
12 组固定版本 Blizzard 源码契约均通过。发布工作流会重复执行这些检查及
安装包校验。安装包只包含插件运行文件。

安装：下载 `FishingBuffTracker-v0.4.0.zip` 或 tar.gz，解压后将
`FishingBuffTracker` 目录放入 `_retail_/Interface/AddOns/`，再执行
`/reload`。
